
class Application {
    constructor () {
        this.notes = database.getNotes();
       const buttons = document.querySelector('[data-buttons]');

       this.updateButtons()
    }

    updateButtons () {
        // очищает содержимое баттанов
        this.buttons.innerHTML = '';
        
        const buttonElement = 
    }
};